export interface ImageReadType {
  id: string;
  imageData: string;
}

export interface ImageCreateType {
  imageData: string;
}

export interface ImageUpdateType {
  imageId: string;
  imageData: string;
}
